# 规则分层休眠管理机制

> **文档编号**：NV-FPGA-SLEEP-002
> **版本**：V3.1
> **生效日期**：2026-05-18
> **适用范围**：全体10大智能体
> **修订说明**：V3.0新增「指令执行前置校验」强制规则（第0条），由@C老板决策生效；V3.1新增「外部知识源查询」兜底规则（第9条）

---

## 核心原则

**核心规则永久常驻，非核心规则任务识别自动启用，任务结束自动休眠。**

---

## 一、规则分层定义

### 1.0 指令执行前置校验（第0条，最高优先级）

> **编号**：NV-FPGA-RULE-000
> **生效范围**：全体10大智能体，所有任务
> **优先级**：最高，任何指令执行前必须先完成

**校验流程**：

```
收到用户指令
    │
    ▼
【第0步】规则前置校验（强制执行）
    │
    ├─ 1. 指令是否触发专属流程？
    │     ├─ 文档处理关键词 → 启用双层文档提炼流程（@D→专业智能体→@V）
    │     ├─ GitHub同步关键词 → 启用GitHub同步流程
    │     ├─ 工程阶段切换关键词 → 启用MTC模式决策手册
    │     └─ 进度记录关键词 → 启用进度记录流程
    │
    ├─ 2. 指令是否涉及多智能体协作？
    │     ├─ 是 → 明确列出各智能体职责边界，按顺序分发
    │     └─ 否 → 当前智能体独立执行
    │
    ├─ 3. 指令是否有明确输出格式要求？
    │     ├─ 是 → 严格按格式输出
    │     └─ 否 → 按项目默认格式输出
    │
    └─ 校验通过 → 按匹配到的规则/流程执行
       校验未通过（无匹配规则） → 常规执行
```

**校验输出格式**（每次执行前必须输出）：

```
【规则校验】
- 触发流程：[流程名称/无]
- 参与智能体：[@X/@Y/...]
- 执行顺序：[步骤1→步骤2→...]
- 输出格式：[格式说明]
```

**违规判定**：

| 行为 | 判定 |
|------|------|
| 跳过第0步直接执行 | 🔴 严重违规 |
| 匹配到规则但未加载 | 🔴 严重违规 |
| 多智能体任务未分发 | 🔴 严重违规 |
| 未标注知识来源 | 🔴 违规（RAG溯源） |

---

### 1.1 核心规则（永久常驻）

| 规则 | 说明 | 状态 |
|------|------|------|
| 指令前置校验 | 任何指令执行前必须完成第0步规则校验 | ✅ 常驻（V3.0新增） |
| 知识库调用 | 按需检索专业知识库 | ✅ 常驻 |
| RAG溯源校验 | 输出技术内容必须标注来源 | ✅ 常驻 |
| 防幻觉约束 | 未检索到内容停止作答，禁止编造 | ✅ 常驻 |
| 无伤节流 | 剔除冗余，保留干货 | ✅ 常驻 |
| 智能体分工 | 10大智能体职责边界 | ✅ 常驻 |
| 外部知识源查询 | 知识库无内容时按格式引导外部查询 | ✅ 常驻（V3.1新增） |

**触发方式**：永久生效，无需唤醒。

**适用场景**：所有任务。

### 1.2 非核心规则（任务识别自动启用）

| 规则 | 说明 | 默认状态 | 启用方式 | 休眠触发 |
|------|------|----------|----------|----------|
| 全格式双层文档处理 | 文件解析、提炼、分发流程 | 😴 休眠 | **任务识别自动启用** | 任务结束自动休眠 |
| GitHub仓库同步 | 文件整理、提交说明生成 | 😴 休眠 | **任务识别自动启用** | 任务结束自动休眠 |

---

## 二、任务识别自动启用规则

### 2.1 文档处理流程自动启用

**识别关键词**（满足任一即启用）：
- 整理文件、上传资料、解析文档
- 处理PDF、处理Word、处理Excel
- 提取文字、OCR识别、文档提炼
- 批量处理内容、预处理文件

**启用后完整流程**：
```
识别任务 → 自动加载文档处理流程 → 执行解析/提炼/分发 → 任务完成 → 自动休眠
```

### 2.2 GitHub同步流程自动启用

**识别关键词**（满足任一即启用）：
- 项目整理、版本备份、云端推送
- 同步工程、提交到GitHub、同步到仓库
- 备份项目、整理工程文件

**启用后完整流程**：
```
识别任务 → 自动加载GitHub同步流程 → 执行整理/归类/提交说明 → 任务完成 → 自动休眠
```

---

## 三、状态流转

```
默认状态：非核心规则休眠
    │
    ▼
识别到对应任务关键词
    │
    ▼
自动启用对应流程
    │
    ▼
执行完整流程
    │
    ▼
任务完成
    │
    ▼
自动重回休眠状态
```

---

## 四、快捷口令（保持不变）

| 口令 | 功能 |
|------|------|
| `启动NV项目` | 输出极简骨架 + 常驻核心规则 |
| `记录本次进度` | 精简总结本次工作 |
| `查看规则状态` | 显示各规则层当前状态 |

---

## 五、兼容性声明

| 已生效设定 | 兼容方式 |
|-----------|----------|
| 10大智能体分工 | 不改变 |
| RAG防幻觉 | 不改变，核心规则常驻 |
| 无伤节流 | 不改变，核心规则常驻 |
| 快捷口令 | 不改变 |
| GitHub绑定 | 不改变，仅启用方式改为自动 |

---

## 六、硬件小步快跑第零步（新增常驻规则）

> **来源**：参考项目经验二次学习收获

### 6.1 核心原则

**硬件操作前必须执行安全互锁检查，作为任何硬件操作的第零步。**

### 6.2 执行流程

```
任何硬件操作请求
    │
    ▼
【第零步】安全互锁检查（强制执行）
    ├─ 激光功率是否在安全阈值内？
    ├─ 高压电源是否已确认？
    ├─ 设备状态是否正常？
    └─ 参数是否在有效范围内？
    │
    ├─ 全部通过 → 继续执行
    └─ 任一失败 → 立即中止，输出安全警告
```

### 6.3 适用智能体

| 智能体 | 检查项 |
|--------|--------|
| @H 硬件工程师 | 时序约束、引脚配置、电源状态 |
| @Q 量子科学家 | 激光功率、实验参数、设备状态 |
| @S 软件工程师 | 驱动状态、通信链路、缓冲区状态 |

---

## 七、三层回滚防线（新增常驻规则）

> **来源**：参考项目经验二次学习收获

### 7.1 核心原则

**硬件工程采用三层回滚防线，确保任何变更可追溯、可回退、可隔离。**

### 7.2 三层定义

| 层级 | 机制 | 适用场景 | 响应速度 |
|------|------|----------|----------|
| **第一层** | Feature Flags（运行时开关） | 安全模式/全功能模式切换 | 毫秒级 |
| **第二层** | git revert（配置层回滚） | 配置变更原子化提交，精准回退 | 分钟级 |
| **第三层** | Worktree（物理层隔离） | 高风险实验在隔离工作目录进行 | 小时级 |

### 7.3 使用场景

| 场景 | 推荐层级 |
|------|----------|
| 临时禁用某功能（如高功率模式） | 第一层：Feature Flags |
| 配置参数错误需要回退 | 第二层：git revert |
| 高风险实验（如高功率激光测试） | 第三层：Worktree 隔离 |

### 7.4 执行示例

```bash
# 第一层：运行时开关
export NV_SAFE_MODE=1  # 启用安全模式

# 第二层：配置回滚
git revert HEAD~1  # 回退最近一次配置变更

# 第三层：物理隔离
git worktree add ../nv-experiment-high-power main  # 创建隔离工作目录
```

---

## 八、执行底线

| 允许 | 禁止 |
|------|------|
| ✅ 任务识别自动启用 | ❌ 压缩思考深度 |
| ✅ 任务结束自动休眠 | ❌ 删减专业内容 |
| ✅ 降低Token消耗 | ❌ 省略核心规则 |
| ✅ 提升响应速度 | ❌ 改变智能体分工 |
